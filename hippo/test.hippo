val something_using_useAt =
  verified theorem dLs { ==> 1+2=3 -> [x:=3;]1+2=x }
  by backward {
    core.ImplyRight(1)[];
    belle.useAt(core.assignb, /*seqpos*/1, /*posinexpr*/list(), /*key*/list(0))[];
    core.Close(-1, 1)[];
  };

print(something_using_useAt);
