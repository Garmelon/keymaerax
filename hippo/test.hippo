print("Hello world!");
print("Hello world\nwith newline!");
print();

val foo = import "foo.hippo";
print(foo.greeting, " from another file!");
val bar = import "foo.hippo";
print(bar.greeting, " from another file (cached, hopefully)!");
print();

var i = 0;
while (i < 10) {
  print("while ", i);
  i = i + 1;
};
print("after ", i);
